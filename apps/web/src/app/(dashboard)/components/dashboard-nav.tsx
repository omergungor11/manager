'use client';

/**
 * DashboardNav is no longer used as a top navigation bar.
 * Navigation is now handled by:
 *   - Sidebar (apps/web/src/app/(dashboard)/components/sidebar.tsx)
 *   - TopHeader (apps/web/src/app/(dashboard)/components/top-header.tsx)
 *
 * This file is kept as a stub to avoid breaking any existing imports
 * during the transition.
 */
export default function DashboardNav() {
  return null;
}
